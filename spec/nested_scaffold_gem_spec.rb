require "spec_helper"

describe NestedScaffoldGem do
  it "has a version number" do
    expect(NestedScaffoldGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
